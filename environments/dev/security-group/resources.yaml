spoke-vpc-default-sg:
  vpc_name: spoke-vpc
  description: Default security group for spoke-vpc
  ingress_rules:
  - from_port: 22
    to_port: 22
    protocol: tcp
    cidr_blocks:
    - 0.0.0.0/0
    description: SSH access
